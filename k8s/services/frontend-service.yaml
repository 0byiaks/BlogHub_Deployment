apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: bloghub
  labels:
    app: frontend
    tier: frontend
spec:
  type: LoadBalancer  # Changed to LoadBalancer for external access
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend

