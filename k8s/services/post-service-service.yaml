apiVersion: v1
kind: Service
metadata:
  name: post-service
  namespace: bloghub
  labels:
    app: post-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: post-service